# devlog-llm — 기획서

> `STRATEGY.md`가 "무엇을 만들지"라면, 이 문서는 "왜 만들고 무엇을 성공으로 볼지"를 정의한다.

---

## 이 프로젝트가 검증하려는 가설

> **AI를 잘 쓰는 패턴은 기록하고 분석하면 재사용 가능한 지식이 된다.**

지금까지는 AI와 대화하고 나면 그 판단 과정(왜 그렇게 물었는지, 어디서 AI가 틀렸는지, 어떻게 바로잡았는지)이 휘발됐다.
이걸 기록·분석·재주입하는 루프를 직접 만들어서, "AI를 잘 쓰는 것"이 감이 아니라 관찰 가능한 패턴인지 검증한다.

---

## 진짜 목표 — 기능 완성이 아니다

이 프로젝트의 산출물은 **코드가 아니라 나의 사고 패턴 변화**다.

- 목표: "블로그 기능 다 만들었다"가 아니라 "가설 세우고 → 최소로 만들고 → 써보고 → 기록하고 → 다음 가설로 넘어가는" 루프를 몸에 익히는 것
- 이 루프 자체가 "프로덕트 엔지니어처럼 일하는 것"의 실습 대상

---

## 유일한 사용자 = 나 자신

- 사용자가 없으므로 "사용자 리서치"는 곧 **셀프 회고**다
- 기능 추가 기준은 상상 속 니즈가 아니라, **이번 주에 실제로 아쉬웠던 지점**에서만 출발한다
- "이런 기능도 있으면 좋겠다"는 추가하지 않는다 — "이게 없어서 이번 주에 실제로 불편했다"만 추가한다

---

## 성공 기준 (기능 목록이 아니라 지표)

사용자가 없는 개인 프로젝트에서 "성공"을 기능 완성으로 정의하면 오버엔지니어링으로 샌다.
대신 아래를 관찰한다.

| 지표 | 확인하려는 것 |
|---|---|
| 이번 주에 블로그 글을 실제로 썼는가 | 기록 습관이 실제로 붙는가 (툴이 아니라 습관이 먼저) |
| 기록한 대화 중 "다시 봐도 쓸모 있다"고 느낀 비율 | 수집 자체가 의미 있는 신호를 남기는가 |
| 그 기록을 보고 실제로 프롬프트/작업 방식을 바꾼 적이 있는가 | 되먹임 루프가 진짜로 도는가 (이게 없으면 이 프로젝트의 핵심 가설이 틀린 것) |
| 만든 기능 중 실제로 다시 쓴 것 vs 한 번도 안 쓴 것 | 만들기 전에 검증 없이 확장했는지 체크 |

---

## 프로덕트 엔지니어 원칙 (진행 중 계속 지킬 것)

1. **가설 → 최소 구현 → 직접 사용 → 기록 → 다음 가설.** 이 순서를 건너뛰지 않는다.
2. **미리 설계하지 않는다.** 필요해질 때, 필요한 만큼만 추가한다 (YAGNI).
3. 진행 판단 기준은 "완성했는가"가 아니라 **"이번 주에 실제로 썼는가"**.
4. 매 사이클마다 `docs/devlog/`에 기록한다 — 무엇을 시도했고, 뭐가 맞았고 뭐가 틀렸는지.
5. AI(Claude)가 제안하는 기능 확장은 기본적으로 의심한다. **내가 실제로 필요해서 요청한 것만** 구현한다.

---

## MVP 재정의

`STRATEGY.md`의 Phase 1은 "블로그 뼈대"였지만, 가설 중심으로 다시 보면 MVP는 더 작다.

> **MVP = 글 하나를 쓰고 → AI와 나눈 대화 하나를 기록하고 → 그 회고를 devlog에 남기는, 가장 작은 루프 하나**

이 루프가 최소 1~2주 실제로 굴러가는 걸 먼저 확인해야, 그다음 단계(RAG, 챗 UI)를 만드는 게 의미가 있다.
루프가 안 돌면 — 즉 내가 실제로 안 쓰면 — 그 시점에 왜 안 썼는지가 다음 가설이 된다.

---

## 다음 액션

- 지금 만든 `apps/be`(Go)에 화려한 기능을 더 얹지 않는다
- 대신: **글 1건을 실제로 DB에 넣고 조회하는 가장 작은 경로**만 먼저 뚫는다 (API 하나, 필드 최소한)
- 그 경험(막힌 점, 걸린 시간, 판단 근거)을 `docs/devlog/be.md`에 그대로 기록한다
- 그 다음에야 FE 에디터/AI 수집으로 넘어간다
